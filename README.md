# حساب استهلاكات السواقط (نسخة Flat)

- كل الملفات في الجذر لسهولة الرفع على GitHub/Streamlit.
- حدد `streamlit_app.py` كـ Main file path.

## تشغيل
```bash
pip install -r requirements.txt
streamlit run streamlit_app.py
```
